# Project context — technical and system facts specific to this project
# that inform design decisions.
#
# Each context item has:
#   id          - Unique identifier (e.g. CTX-PROJ-001)
#   description - One-liner factual statement about the current system

contexts:
  - id: CTX-KB-001
    description: "Any AI agent with file-write access works: Claude Code, Cursor, GitHub Copilot, ChatGPT with file tools"
  - id: CTX-KB-002
    description: "No separate LLM API or script needed — the agent IS the AI pipeline"
  - id: CTX-KB-003
    description: "Repo is the knowledge base: markdown files committed to git = persistent, versioned, portable"
  - id: CTX-KB-004
    description: "Obsidian can open any local folder as a vault — the repo folder works out of the box"
  - id: CTX-KB-005
    description: "Most AI agents auto-load an instruction file from the repo root (CLAUDE.md, AGENTS.md, .cursorrules)"
  - id: CTX-KB-006
    description: "AI agents can read all existing notes before writing — enabling dedup and category consistency"
  - id: CTX-KB-007
    description: "GitHub supports template repos — 'Use this template' creates a new repo from it with a custom name, no CLI needed"
  - id: CTX-KB-008
    description: "GitHub CLI supports: gh repo create my-kb --template owner/wombat-wiki — one command, local clone included"
  - id: CTX-KB-009
    description: "npm create convention (npx create-wombat-wiki my-kb) is a standard zero-install scaffolding pattern for developers"
  - id: CTX-KB-010
    description: "degit scaffolds from a git repo locally without copying history — lightweight alternative to cloning a template"
